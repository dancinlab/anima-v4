# Research-gate — mech-6 제약 시장 (constraint-market): DON'T-OPEN

**Date**: 2026-07-17 · **Cost**: $0 (closed-form on the sealed record — no run, no download)
**Verdict**: **DON'T-OPEN.** No H_008 is created for mech-6. The mechanism map is now fully disposed.
**Classification**: TWIN-REDUNDANT with mech-1 (primary) · learned staging SUBSTRATE-WALLED (H_005/G3-b
transferred) · curriculum staging SATURATION-WALLED (H_007 transferred). Walled = UNREACHED, not falsified.

This is the admissibility gate returning its verdict before any spend, exactly as it did for mech-3
(H_001), G3-b/H_006 (REGATE_g3b), and mech-2/4 (H_007 seed §0). Every number below is cited with its
arm and source path (adm.4).

---

## 0. What mech-6 claims, decomposed

Per `ARCHITECTURE.json components/mech-6.constraint-market`: N>2 specialist engines (negation scope,
honorific agreement, tense, argument binding) each publish a constraint field; "G" is a **learned
AUCTIONEER** resolving conflicts into a max-satisfaction assignment; loss = CE(decoder | winning
assignment) + per-engine violation penalty; **specialists receive gradient when violated (N-way
gradient)**; the crux (`mech-6.l2-defense`) is a conflict-forcing curriculum "so the trunk alone cannot
lower CE on conflict sentences."

Decomposed against the sealed record, mech-6 is a composite of exactly three parts, each already disposed:

| mech-6 part | Which sealed result already owns it |
|---|---|
| Superposition-of-N-fields FORMAT, consumed by the trunk | mech-1 SEALED — H_004's G-2 field was ALREADY rank-6 per item (K=6 MULTI-BIND, T = Σ c_k M_k; off-top rank-mass 0.8333 pre-run, F4 offtop 0.6467/0.6963 trained — `ng.mech1.multibind-g0g1-pass`, `verdict.json`). N>2-as-format is inside the seal. |
| LEARNED auctioneer / learned specialists (the distinctive content) | H_005 K3 + REGATE_g3b — the drill-identified trained-module wall (values 0.71–0.82 vs hand 1.0; fit instability 1.0/0.832; bigger module worsens identification). |
| N-way gradient / per-engine violation penalty | H_007 RETIRED — gradient-conflict-as-forge unmeasurable here (control saturation), and P-conflict role-specificity REFUTED (C-shuf +0.539 > A-tug +0.329, `g2_full.out`). |

---

## 1. The twin-redundancy test (fd.family-structure-conflict)

The audit's flag is confirmed, and it is TIGHTER than the audit knew: mech-1's sealed measurement was
not N=2-shaped. The MULTI-BIND panel stacks six constraint conjuncts and the sealed F1/F4 verdict is
precisely "a **superposition of 6 constraint fields**, as a field, causes held-out binding its low-rank
compression cannot" (F1 Δ(A-duel−A-rank1) = 0.3789/0.3802; A-rank1 = the compression arm). So the
question "does a superposition of MORE THAN TWO constraint fields carry thought beyond its summary?" is
not open — it is H_004's verdict verbatim. What the six conjuncts share (one constraint TYPE) vs
mech-6's heterogeneous specialists varies field CONTENT, not the resolver verb. Same axis
(`fd.true-axes`: structure-conflict-resolution = {1, 6}).

**The genuine N>2-only residue exists mathematically** — and must be named honestly: joint
non-decomposability. With N=2 the resolved assignment IS the pairwise resolution; with N≥3 one can
build constraint systems that are pairwise satisfiable but jointly unsatisfiable (2-SAT/3-SAT gap), or
cyclic preference structures (Condorcet), where the max-satisfaction assignment carries bits no bank of
pairwise duels holds. That is a real capability class N=2 cannot express. The question is whether it
has an ADMISSIBLE falsifier on this substrate. It does not — §2.

---

## 2. The three horns (why no admissible falsifier exists at 3.7M/byte/Korean/templatic)

**Horn A — hand-staged market (joint resolution hand-computed, injected as a field).** The natural
lever is A-market (joint max-sat field) vs A-pairsum (bank of pairwise resolutions). Two sub-cases,
both degenerate:
- *Panel where pairwise determines joint* (stacked independent constraints — H_004's shape): A-pairsum
  receives the same resolved bits as A-market ⇒ Δ = 0 by construction; the contrast is vacuous and the
  claim collapses into H_004's sealed FORMAT verdict. Twin-redundant.
- *Panel with a genuine cycle* (pairwise does NOT determine joint): then A-market's input CONTAINS the
  gold bits and A-pairsum's does not — a MORE-GOLD-vs-LESS-GOLD contrast, mech-2 T1's exact degenerate
  shape (H_007 seed §0). Its green is guaranteed by the sealed record (delivered resolution is consumed
  at ceiling — H_004 A-hand fit 1.0/1.0; the trunk cannot compose the withheld bits unaided — measured
  0.49–0.62 band), so DEAD is unreachable in substance ⇒ fails "both answers reachable"
  (verification.admissibility-gate). It certifies information delivery, not a market.
- The only non-degenerate hand form — both arms receive raw CONSTRAINT fields (not resolutions) and the
  auctioneer itself is the one variable — forces the auctioneer to be a real component: hand-staged
  auctioneer = delivering the answer again (previous bullet); learned auctioneer = Horn B. The vice closes.

**Horn B — learned auctioneer/specialists (mech-6's actual distinctive content).** The verdict path
contains a drill-identified trained module reading fields/trunk state through drill-CE — G3-a's measured
channel, and STRICTLY HARDER than H_005's χ̂: an N-way max-satisfaction resolution vs a pairwise concord
sign. The measured record transfers verbatim (REGATE_g3b §1–2): value ceiling ≤ 0.767 mean / 0.824
best-seed where hand delivers 1.0 (F1a′ margins −0.176/−0.290, both seeds incl. the fit seed); 1-in-2
drill-fit instability on a strictly easier optimization ⇒ expected precond quarantine; the wall is
drill→held-out IDENTIFICATION, so enlarging the auctioneer worsens it. No bar certifiable at 2×
headroom. Additionally, mech-6's "specialists receive gradient when violated" is an N-way
gradient-conflict objective — mech-5's instrument, whose role-specificity H_007 just REFUTED at scale
(the divergence signature is generic "any different second target": C-shuf +0.539 > A-tug +0.329).
INADMISSIBLE on already-measured numbers.

**Horn C — conflict-forcing curriculum, trained-vs-trained (mech-6.l2-defense's own crux).** The crux
demands "the trunk alone cannot lower CE on conflict sentences." H_007's wall statement is
mechanism-agnostic: on a finite drilled templatic alphabet, any supervision channel strong enough to
ground the composition's ingredients (here: N constraint types + their conflict-resolution convention)
is strong enough for the unaided forward trunk at 3.7M/d=384 to learn the whole composition to ceiling
(C-scaf 0.8073 ≥ the 0.60 floor, C-dup 1.0000 — `g2_full.out`). **Predicted G-1.5
(trained-control-ceiling) outcome for any mech-6 conflict panel: control saturation — the
discriminating band is EMPTY.** The only known escape is to carry the gold bits in an injected field
the control cannot see (H_004's escape, C-scaf ≈ 0.5) — which is Horn A.

**mech-6's own pre-registered falsifiers fail the current gates as written** (`mech-6.falsifier`):
L1 "eff-rank of the winning assignment ≥ active engine count" fails adm.2-not-free — H_007 measured F4
(eff-rank ≥ 2) passing in BOTH arms with F5 Δ_projF4 = 0.0000 in both: rank is a free ingredient the
zero-mechanism control also exhibits. L2 "mean-pool substitution ΔCE > 0.02 on conflict sentences" is a
CE-scored verdict — p7 (verification.p7-retained) bars CE as truth; ΔCE is a DEAD-check form (H_004 F5),
not a primary falsifier.

---

## 3. Verdict and campaign consequence

**DON'T-OPEN mech-6.** Classification, precisely: the N>2 superposition FORMAT is **twin-redundant**
(inside H_004's sealed rank-6 measurement); the one genuine N>2-only capability (joint
non-decomposability) has **no admissible falsifier** (hand-staging degenerates to
more-gold-vs-less-gold / Δ=0; the non-degenerate form needs a learned auctioneer); the learned
auctioneer is **substrate-walled** (H_005/G3-b transferred); the conflict-curriculum staging is
**saturation-walled** (H_007 transferred, G-1.5 outcome predicted: control saturates). UNREACHED, not
falsified. Reopen conditions are IDENTICAL to the one wall list, recorded once (H_007 seed §0): ≥8M
params and/or jamo/word substrate passing an F1a′-analog first · a non-templatic register with probe
φ→gold < 1.0 (needs a new heuristic-audit method) · an external-oracle premise (a different campaign).

**The map is now fully disposed**: mech-1 🟢 SEALED (H_004 FIELD-FORMAT + K3's measured 0.18–0.29
cost-of-removing-the-hand) · mech-2/4 🧱 walled · mech-3/7 ☠️ dead · mech-5 ⚫ retired-inadmissible ·
mech-6 ⛔ don't-open (this record). H_006 stays burned; H_007 stays retired.

**mech-1's sealed FIELD-FORMAT result is the campaign's terminal positive finding at this substrate.**
The honest next moves, in order:
- **[c] Write up mech-1 as the campaign verdict** — the A⇄G premise survives in exactly one measured
  form: the resolved tension FIELD as causal carrier (Δ 0.379/0.380, F4/F5 clean), hand-staged χ, with
  the trained-component walls honestly scoped. This write-up is now unblocked by any open mechanism.
- **[b] The scarce-answer sample-efficiency hypothesis** (H_007 reopen-1) is the ONE genuinely non-twin
  residue on the board: it varies the SUPERVISION BUDGET — an axis none of mech-1..7 occupied — and its
  claim ("tension accelerates OOD acquisition under rationed answer supervision") is new in claim-space,
  not a re-run of a sealed verb. Pre-registrable as a NEW H with the G-1.5 band designed in from the
  start: the few-shot budget must have a MEASURED plateau where the trained control sits stably
  sub-ceiling across seeds (band existence proven before mechanism arms), pricing in H_007's own warning
  that tuning to the control's learning-curve knee is fragile at the measured 0.161 cross-seed spread.
  If the band-existence pre-run fails, [b] closes at near-$0 and [c] stands alone.

## Honest limits

1. Horn A's "Δ=0 on stacked-independent panels" and "Δ forced on cyclic panels" are information-
   containment arguments, not measurements — but they are H_001's vacuity shape applied at design time,
   which is exactly what the admissibility gate is for; a concrete cyclic-panel design that evades both
   sub-cases without a learned resolver would reopen Horn A and should be brought as a challenge to
   this record.
2. Horn C's G-1.5 prediction (control saturates) is a prediction, not a run — it is falsifiable for
   ~1.5h of MPS time IF someone first produces a Horn-A-clean panel; absent that panel the prediction
   is unpriced and the gate stays closed on Horns A+B alone.
3. The H_005/G3-b ceiling transfer assumes the auctioneer reads the same trunk-state/field interface
   under drill-CE at this scale; an auctioneer trained under a corpus-scale objective (not drill-CE)
   would evade Horn B's arithmetic — but then its curriculum is Horn C, and no third supervision
   channel is known (H_007 honest-blocker 3).
4. "Terminal positive finding" is scoped to THIS substrate (3.7M/byte/Korean/templatic) and this
   campaign's admissibility discipline — not a claim about constraint markets at scale.
