# H_003 G-1 grammar verdict — LLM authoritative (owner-delegated "LLM 판정")

**VERDICT: PASS.** The negation structure is grammatically well-formed across every
cell and every verb in the frozen inventory.

## What was judged (all unique verb×cell surfaces from f2'/f1'/f2b + drill)
- `-지는 않았다` long negation (A-family) — well-formed.
- `-(으)ㄴ 것은 아니다` cleft negation (K-family) — well-formed.
- compound split `{N} [slot] 하지는 / 한 것은` — adverb insertion well-formed.
- bound 못 `-지 못한 것은 아니다` (Kb2 diagnostic) — well-formed.
- slot adverbs (잘/더/꽤 fillers · 안/못 negators) sit in a grammatical pre-verb position.
- The three swapped verbs are 못-compatible: 감상 못 하다 · 야유 못 하다 · 못 참다 all grammatical.

## Honest caveats (documented; neither reverses the verdict)
1. **Subject-semantics mismatch.** `이 영화 웃다` reads as "the movie laughs", odd for
   viewer-action verbs. But this is v1's VALIDATED synthetic frame, and it is IDENTICAL
   across A-atom and A-shat (only the codec encoding differs), so it cannot produce a
   differential Δ — it is arm-symmetric and non-confounding. Acceptable for a synthetic
   byte-level negation-recombination drill.
2. **더-filler** ("더 웃지는" implies a comparison baseline) — grammatically valid; kept
   for CPT frequency (the design chose 잘/더/꽤 for frequency-match, not naturalness).

## Consequence
The G-1 operator gate is CLEARED by authorized LLM judgment. All naturalness residue is
arm-symmetric, so it does not threaten the atomicity contrast. frozen_at may be stamped.
