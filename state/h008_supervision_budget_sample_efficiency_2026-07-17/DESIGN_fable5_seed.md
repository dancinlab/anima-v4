All grounding in hand — the TERMINAL campaign result (`state/campaign_result_2026-07-17/CAMPAIGN_RESULT.md`),
H_007's full retirement record (`state/h007_gradient_tug_role_code_drill_2026-07-17/` — seed, G-0 builder,
G-2 run, `verdict_g2.json`, card), and the standing gates (`verification.trained-control-ceiling`,
admissibility's two halves, recomputed free-slot codebook audit). Here is the pre-register gate for the ONE
non-twin reopen door the campaign left: supervision-budget / sample-efficiency.

---

# Research-gate seed — H_008 supervision-budget: the ANSWER-BUDGET drill

**SELECTED: OPEN H_008 — gated, controls-first, kill-capable at ~1.5h wall / $0 cash.** The near-$0 CLOSE
was checked seriously (§0) and is not taken: every closed-form close-argument either narrows the design
instead of closing it, or is exactly the empirical question the mandatory G-1.5 band-existence pre-run
referees before any mechanism arm trains. The campaign stays CONCLUDED unless and until every gate below
is green; mech-1 stays SEALED, H_006 stays BURNED, mech-5 stays RETIRED, mech-6 stays DON'T-OPEN.

## 0. SELECT — the close-case, weighed argument by argument

The hypothesis: **under a scarce, arm-identical few-shot answer budget, the objective-conflict arm (A-tug)
reaches the held-out SWAP-XOR composition at higher OOD d_acc than the compute/param/gradient-matched
duplicate-objective control (C-dup) at the same frozen budget.** This varies the answer-supervision BUDGET
— an axis no disposed mechanism occupied (all seven varied tension CONTENT; `fd.true-axes`) — and it is
mech-5-reborn ONLY IF the band pre-run fails and we run anyway, which the gates below forbid.

Four close-arguments were checked before opening:

1. **"Partial-coverage rationing is closed-form self-defeating"** — TRUE, and it narrows the design
   rather than closing it. An f2 subject lexeme whose class is never answer-grounded is an arbitrary bit
   reachable by NO arm (H_005's ungrounded⇒all-arms-0.5 mode), while the covered fraction saturates
   (C-dup f2 1.0000 s0, `verdict_g2.json`) — so a coverage-rationing knob forces F1 Δ≈0 at every budget.
   Consequence absorbed in §2: the knob must hold class COVERAGE complete and ration EXAMPLES-PER-KNOWN-
   SUBJECT; the band must come from rule/binding sample-complexity, not from ungrounded bits.
2. **"H_005's probe φ→hon=1.0 templatic ceiling says the trunk memorizes the register — rationing just
   slides one curve"** — does NOT transfer. That ceiling (G3-0d, H_005 card) is about SURFACE-readable
   drilled bits. The SWAP-XOR class bit is absent from the surface by construction (pools balanced,
   `build_swap_xor.py` balance/decorrelation audits); it enters the trunk only through answered examples
   — so the budget rations a REAL information channel, PROVIDED the panel has no side channel (§0.5: the
   H_007 panel HAS one — fixed by the §3 rebuild, not by closing H_008).
3. **"The band is a fragile knee that seeds will straddle"** — the strongest close-argument, and it is
   priced, not assumed. The band the arithmetic needs is [0.60, 0.80] (§4), width 0.20, against measured
   per-arm cross-seed spreads of 0.0677 (C-dup 1.0000→0.9323), 0.1354 (C-shuf 1.0000→0.8646), 0.1458
   (C-scaf 0.8073→0.9531) on H_007's G-2 (`verdict_g2.json`) and 0.0885/0.0729 (A-atom 0.5677→0.6562,
   A-shat 0.6823→0.6094) on H_003 (`state/h003_.../verdict.json`); H_003's Δ-spread 0.1614 is the priced
   worst case. Sharp data-fraction transitions on algorithmic/templatic tasks (grokking literature, §1)
   make an empty or unstable band a LIVE outcome — that is K1, decided by a ~1.5h seed-0 sweep BEFORE any
   mechanism arm, and a K1 close is a recorded, measured wall, not a wasted run.
4. **"Role ingredients are free, so both arms ride the same curve — predicted null"** — plausible, and
   K2 is honestly the modal outcome. H_007 measured F4 passing in BOTH arms at full budget (A-tug probes
   subj/obj 0.9655/0.8621, C-dup 1.0000/0.7586, s0) with F5 Δ_projF4 = 0.0000 both — the zero-conflict
   control carried the same probe-readable role ingredients. BUT that read was taken at FULL answer
   supervision (~333 epochs × 384 answered items); whether the control's ingredients survive a 16×-rationed
   budget is exactly what the experiment measures, and a K2 red UPGRADES the mech-5 family from
   UNMEASURED (retired-inadmissible, zero bits) to MEASURED-NULL — the only remaining unmeasured axis
   becomes measured either way. A clean red is worth sealing (H_007 K2's own language).

**Why OPEN wins**: every failure mode has a pre-registered KILL, not a rescue; the first kill is available
at ~1.5h wall / $0 cash; both non-green outcomes strictly improve the record (K1 = the reopen door closes
measured; K2 = the budget axis closes measured); and a green would be the campaign's first measured
positive on the AUTONOMY side of mech-1's PARTIAL verdict — an objective (not a hand) buying measurable
structure — in its weakest, most honest form: sample-efficiency, claimed as sample-efficiency and nothing
more (scope rule K-scope, §6).

## 0.5 NEW measured fact (2026-07-17, this gate) — the H_007 panel cannot carry H_008

Computed from the frozen artifacts (`swap_xor_f2.json` + `train_g1_smoke.build_drill()`, reproducible
one-liner recorded here): the f2 panel's subject lexemes are {사장, 교수, 학생, 친구} but the 384-item
drill's subject lexemes are {교수, 부장, 회장, 친구, 동생, 이웃} — **사장 and 학생 have ZERO answered
subject occurrences in the entire drill**, yet C-dup.s0 scored f2 = 1.0000 (arm C-dup,
`state/h007_gradient_tug_role_code_drill_2026-07-17/verdict_g2.json`), acing ~half the panel whose gold
depends on class(사장)/class(학생). Since gold never constrains OBJECT class, their class reached the
trunk through a **budget-INDEPENDENT side channel**: shared-syllable byte families (사장~부장/회장 share
장; 학생~동생 share 생 — real Korean status morphology) and/or NSMC CPT distributional semantics of these
REAL nouns. For H_007's F1 this was arm-symmetric and moot (its kill was F3/headroom); for H_008 it is
disqualifying: an answer-budget knob on this panel rations only PART of the class-grounding channel, so
the band would measure an uninterpretable mixture. **Consequence: §3 rebuilds the panel (SWAP-XOR-B) with
byte-orthogonal synthetic pseudo-noun pools and f2-subjects ⊆ drill-subjects; the full G-0 audit suite is
re-run plus a new A7 class-byte-orthogonality audit.** This finding also retro-sharpens H_007's root
cause (part of its control saturation ran through byte-family generalization, not only direct
supervision) — recorded here, no relitigation: mech-5 stays retired.

## 1. Literature scan (direct bearings; MEMORY rows must pass the three-scan verify before freeze)

| Evidence | Bearing | tag |
|---|---|---|
| Semi-supervised / aux-self-supervision under label scarcity (ladder networks Rasmus 2015; ULMFiT/BERT few-shot fine-tuning lines) | Auxiliary unlabeled-signal objectives helping MOST when labels are scarce is the field's best-attested effect — the boring explanation is STRONGER for H_008 than it was for H_007. C-shuf (placebo) is therefore co-primary and its headroom is priced BEFORE the mechanism arm (G-1.5c, K1b). | MEMORY |
| Grokking / sharp data-fraction generalization transitions on algorithmic tasks (Power et al. 2022) | The main threat to band EXISTENCE: control f2(k) may be a cliff, not a knee. Makes K1 live; motivates the plateau + adjacent-point + cross-seed stability requirements (§5). | MEMORY |
| Compositional generalization under limited primitives (SCAN, Lake & Baroni 2018 + successors) | Held-out recombination from few examples is the canonical hard case; mixed results ⇒ DEAD reachable. | carried (H_007 §1, lit-verified 2026-07-17) |
| Gradient-conflict-as-pathology (PCGrad 2001.06782 · GradNorm 1711.02257 · Recon 2302.11289) | The contrarian premise (conflict as forge) — already lit-VERIFIED for H_007; carried unchanged. | VERIFIED (H_007 lit-verify) |
| Caruana 1997 (MTL as inductive-bias transfer) | Prior FOR the mechanism route: an aux objective can substitute for labeled examples by building shared features — but it predicts C-shuf helps too; only F6 separates role-ALIGNED transfer from generic transfer. | carried (H_007) |
| H_007 G-2 measured record | The full-budget endpoint and every trained-control anchor this seed cites (C-dup 1.0000/0.9323 · C-scaf 0.8073/0.9531 · C-shuf 1.0000/0.8646 · F4-both-arms · F5=0.0). | measured (`verdict_g2.json`) |
| H_003/H_004 measured bands | Chance-region behavior + seed spread pricing (C-scaf 0.4948–0.5534; Δ-spread 0.1614). | measured (both `verdict.json`) |

Net: the mechanism's sample-efficiency reading has a real literature route (MTL/self-supervision) AND a
real placebo threat from the same literature; the design's job is the F6 separation, and the band's job
is to make F1 carry bits at all.

## 2. The one-variable lever — head-G's target; the budget is HELD CONSTANT and byte-identical

Rig = H_007's G-2 rig verbatim (`train_g2.py` two-head trunk, d=384 L=4 E=3, CPT 3000 steps on NSMC
windows + drill 4000 steps batch 32, head-A forced-choice free-slot d_acc eval, heads dropped at eval),
with ONE new constant and ONE fix:

- **The answer budget B(k)** — a deterministic, seed- and arm-INDEPENDENT subset of exactly k drill items
  (k = 24·r, one-per-subject-lexeme coverage floor, §3 grid). Items in B(k) train exactly as in H_007
  (`seq = surf + ans`, answer positions in head-A's CE mask). Items NOT in B(k) are **answer-TRUNCATED**:
  the sequence ends at the surface (no answer bytes in tokens, none in any mask) — not merely loss-masked,
  so no gold byte ever enters any arm's input. Uniform batch sampling over all 384 items is kept, so each
  budgeted item still receives ≈ 4000×32/384 ≈ 333 answered exposures REGARDLESS of k — the knob varies
  the number of DISTINCT supervised examples only, the classic sample-complexity axis. head-G's target is
  surface-only (A6 leak ban carried) and is therefore NEVER rationed: the aux signal stays full-strength
  in every arm at every k. The budget schedule, sampling stream, steps, init, and B(k) itself are
  byte-identical across arms.
- **Rejected knob variants** (the gate's own checklist): (i) answer-bearing STEP rationing / curriculum
  cutoff — puts the band on the optimization-TIME axis, i.e. tuning to the control's learning-curve knee,
  the exact fragility H_007's rescue analysis (b) named ("fragile at the priced 0.161 cross-seed spread"),
  plus late answer-free drift (forgetting) as an uncontrolled confound. REJECTED. (ii) partial-coverage
  pair rationing — closed-form self-defeating (§0 arg 1). REJECTED.
- **The fix**: C-perm's permutation becomes a GLOBAL derangement of drill answers (frozen per seed), not
  per-batch — pricing H_007's flagged harness violation (C-perm 0.5938/0.5208 outside [0.45,0.55]).

| arm | head-G target | budget | isolates |
|---|---|---|---|
| **A-tug** | swap(x) role-swapped rendering | B(k*) | the mechanism (conflict arm at scarce answers) |
| **C-dup** | x itself (duplicate forward CE) | B(k*) | **the control** — matched params/compute/gradient volume, zero conflict |
| **C-shuf** | swap(x′), x′ ≠ x, length-aligned | B(k*) | placebo — generic hard-aux transfer vs role-aligned |
| **C-scaf** | none (head-G present, λ_G=0) | B(k*) | scaffold floor at the budget |
| **C-perm** | swap(x) | B(k*), answers globally deranged | harness (no consistent rule ⇒ ≈0.5) |

**What the tension could concretely buy (the mechanism route, stated before running):** head-G's swap
objective pressures the trunk to represent both fillers AND their roles at every position, on ALL 384
surfaces, unrationed. C-dup's aux adds zero new pressure by construction. At scarce k the control must
assemble subject-identification + class-lookup + XOR from k answered examples alone; A-tug's answer-CE
only has to wire class-lookup + XOR on top of a role representation the aux objective already built.
C-shuf trains generic copy/render machinery but never THIS sentence's role binding — so F6 separates the
role-aligned route from generic aux transfer. If the route is real, Δ(A-tug − C-dup) > 0 at the band
budget; if role ingredients come free even at scarce answers (H_007's F4 lesson), Δ ≈ 0 = K2.

## 3. The panel — SWAP-XOR-B (same rule, side-channel-free pools, coverage-complete by construction)

Same grammar, same rule (gold = class(subject) ⊕ form(object-marker)), same fixed byte offsets, same
scrambling counterbalance (SOV/OSV), same markers/verb/answer forms as `build_swap_xor.py`. Three changes,
each forced by a measured defect:

1. **Byte-orthogonal synthetic pseudo-noun pools** (12 honored-class + 12 plain-class, 2 syllables = 6 B
   each): all 48 syllables DISTINCT across the lexicon (no shared-syllable families — kills the 장/생
   byte-family channel of §0.5), none drawn from Korean status morphology (no 장/님/씨/사 finals), and no
   pseudo-lexeme occurs in the CPT corpus (kills the NSMC distributional channel; checked by scan).
   Class becomes a genuinely arbitrary partition groundable ONLY through answered examples — restoring
   the closed-form property the budget knob needs: an unanswered lexeme's class carries zero bits.
2. **Symmetric coverage-complete grid**: drill = 24 subjects × 2 markers × 2 orders × 4 objects = 384
   items (envelope unchanged); f2 = 192 = the SAME 24 subjects × 2 × 2 × 2 with HELD-OUT (subj,obj)
   pairings; f1 = 64 disjoint drilled-distribution pairings. Every f2 subject is a drill subject
   (f2-subjects ⊆ drill-subjects, violated by the H_007 panel per §0.5), so coverage is exact and the
   budget floor is k_min = 24 (each lexeme answered as subject ≥ once).
3. **Budget constructor B(k)**, k = 24·r, r ∈ {1, 4, 8, 16}: per lexeme, r answered items chosen
   deterministic-balanced (markers alternated within lexeme where r ≥ 2; orders and object classes
   round-robin; (cs, marker, order) cells exactly balanced at k/8 each). B(k) ⊆ B(k′) for k < k′
   (nested, so the sweep varies one thing). SHA-256 of each B(k) recorded at freeze.

**G-0b audit suite ($0, exit-0-blocking, all recomputed — never inherited):** A1 free-slot recomputation
(expect [0]; recompute per the standing gate) · A2 GF(2) codebook rank=2, gold ∉ span of single
ingredients · A3 every single-site heuristic = 0.500 exactly (incl. worst-single-byte on the new pools) ·
A4 length parity · A5 swap alignment · A6 leak ban (answer forms absent from head-G targets) · balance +
pairwise decorrelation · held-out-pairing disjointness — the ten H_007 G-0 audits re-run on the new panel
— PLUS **A7 class-byte-orthogonality** (best byte-position/byte-value/syllable→CLASS map over the
24-lexeme lexicon = 0.500 exactly; no lexeme in CPT text) and **B1–B6 budget audits**: B1 coverage (every
f2 subject answered ≥ once at every k in the grid) · B2 within-budget balance (gold/marker/order 50/50,
object class balanced, per-(cs,marker,order) cells equal) · B3 GF(2) identifiability ON B(24) (rank 2,
gold ∉ single-ingredient spans — the rule is determined by the smallest budget) · B4 nestedness B(24) ⊂
B(96) ⊂ B(192) ⊂ B(384)=drill · B5 structural answer-truncation check (no gold byte reachable from any
non-budget item's token stream, any arm) · B6 determinism (B(k) independent of seed and arm; hash match).

## 4. Falsifiers + admissibility pre-check (arithmetic first, anchors measured on THIS panel at THIS scale)

**The bar**: b = 0.10 (SUPPORTED requires Δ ≥ 0.10 both seeds; DEAD < 0.05 both; between = PARTIAL).
Why not H_004's 0.15: the trained-control ceiling condition control ≤ 1 − 2b would then demand ≤ 0.70,
leaving band [0.60, 0.70] — narrower than two of the five measured per-arm cross-seed spreads (0.1354,
0.1458, §0 arg 3), i.e. K1 by arithmetic before running. b = 0.10 gives **band = [0.60, 0.80]**: floor =
chance + 2.8σ = 0.5 + 2.8×0.036 ≈ 0.60 (H_007 F3 convention, σ at n=192), ceiling = 1 − 2×0.10 = 0.80,
width 0.20 — above every measured per-arm spread, with stability still the modal kill and honestly so.
b = 0.10 = 2× the 0.05 DEAD floor keeps the PARTIAL zone meaningful. Note: Δ at n=192 has σ_Δ ≈ 0.051, so
0.10 ≈ 2σ_Δ per seed — the both-seeds rule is what carries the false-positive control, as in H_003/H_004.

- **F1 (primary)**: Δ = d_acc(A-tug, f2) − d_acc(C-dup, f2), both arms at the frozen budget k*.
  SUPPORTED iff Δ ≥ 0.10 both seeds; DEAD iff < 0.05 both; split ⇒ INCONCLUSIVE-CONSOLIDATE (no third
  seed, H_005 precedent). *Reachable*: headroom = 1.0 − E[C-dup@k*] ≥ 0.20 = 2b is GUARANTEED by the
  band condition (E measured in G-1.5 on this panel/scale/seeds — the trained-control-ceiling gate is
  satisfied by construction, not by inherited anchors; 0.62-style fiction structurally impossible).
  *Grounded*: E[C-dup@k*] ≥ 0.60 proves the task reachable at the budget (the anti-H_005 half).
  E[A-tug@k*] honestly UNANCHORED — that is the experiment. *Not-free*: C-dup is trained,
  compute/param/gradient-volume-matched, same B(k*). *One variable*: head-G's target only. *Both answers
  reachable*: DEAD = A-tug ≈ C-dup mid-band — fully live; A-tug < C-dup (conflict HURTS at scarce
  supervision) is also live and informative, recorded as a directional K2.
- **F2 (liveness)**: teacher-forced budget-fit — d_acc on B(k*) items ≥ 0.95 per arm per seed (the arm
  mastered what it was actually taught; H_005 precond discipline). f1 (held-out-pairing liveness panel)
  recorded descriptively, no bar — its budget-scaled anchor comes free from the pre-run's C-dup read.
- **F3 (band-holds, replaces the old not-free floor)**: in the verdict record, C-dup f2 ∈ [0.60, 0.80]
  AND C-scaf f2 ≤ 0.80, both seeds — these ARE the reused G-1.5 runs, so F3 restates the admissibility
  evidence inside the frozen ledger (H_001 discipline: inadmissible ⇒ no verdict, no exceptions).
- **F4 (inherited L1, eff-rank — free-ingredient-trap-aware)**: linear probes on A-tug's held-out trunk
  states decode subject-class AND object-class > 0.65 with |cos(w_subj, w_obj)| < 0.8 (eff-rank ≥ 2);
  FAIL ⇒ DEAD (v1's 1-bit seam) regardless of F1. NEW: the same probes run on C-dup at k*, and the
  MECHANISM-ROUTE claim additionally requires probe(A-tug) − probe(C-dup) ≥ 0.10 on subject-class — at
  full budget H_007 measured the control carrying EQUAL ingredients (C-dup subj 1.0000 vs A-tug 0.9655,
  s0), so an A-tug-specific surplus at scarce budget is what "the aux built it, not the answers" looks
  like; absent surplus, F1-green is scoped to unexplained efficiency (K5-scope), not the role-code route.
- **F5 (inherited L2, ablate — instrument strengthened from H_007's measured Δ=0.0000)**: single rank-2
  projection was measurably too weak (F5 Δ_projF4 = 0.0/0.0104 A-tug, 0.0 C-dup — code untouched or
  redundant). H_008 uses ITERATIVE nullspace erasure (INLP-style): alternate probe→project until both
  class probes ≤ 0.55 or rank = 16; then require Δd_acc(A-tug, f2) ≥ 0.05. Specificity: same-rank random
  projection must be inert on A-tug (> −0.05); the same erasure on C-dup recorded. Erasure target not
  reached within rank 16 ⇒ F5 VOID (instrument limit, claim capped at PARTIAL — honesty over theater);
  erasure reached but Δ < 0.05 both seeds ⇒ the code is epiphenomenal ⇒ no mechanism claim.
- **F6 (placebo, co-primary here)**: Δ = d_acc(A-tug) − d_acc(C-shuf) ≥ 0.05 both seeds — else the green
  reads "generic aux self-supervision buys few-shot composition at 3.7M" (the best-attested effect in the
  scarce-label literature, §1) and NO tension claim is written. Admissibility of F6 itself: E[C-shuf@k*]
  ≤ 0.80 measured in G-1.5c BEFORE any mechanism arm (else K1b — no headroom for the separation).
- **F7 (closed-form)**: the full G-0b suite (10 re-run audits + A7 + B1–B6), exit-0-blocking.
- **Harness**: C-perm (global derangement) f2 ∈ [0.45, 0.55] both seeds — fix priced from H_007's flagged
  0.5938/0.5208.
- **P-conflict (precondition, scope-only)**: differential cos(∇CE_A, ∇CE_G) vs C-dup recorded with full
  trajectory (τ = 0.05 carried). H_007 G-2 REFUTED role-specificity of this instrument (C-shuf
  differential +0.5390 > A-tug +0.3285) — so it can no longer license anything; it only VETOES: absent
  conflict ⇒ any green is written as aux-transfer, no tension language (K4). Role-specificity now rests
  entirely on F6.
- **Preconds carried**: drill-fit as F2 · FLOP/param parity asserted in code · plateau precondition:
  C-dup f2 evaluated at 60%/80%/100% of drill steps, max−min ≤ 0.05 both seeds at k* (the band must be a
  PLATEAU, not a transient — the gate's own wording).

All E-values re-confirmed from G-1.5 before `pre_register_frozen: true`; estimates are not anchors (adm.4).

## 5. Cheapest-first path (controls first — the meta-gate's corollary (iii) made structural)

Cost model from the measured envelope: H_007 G-2 = 10 arm-runs ≈ 7h ⇒ ~0.7h/run (≈0.3h CPT + ≈0.4h
drill). CPT is arm- and budget-independent (pure corpus phase, `train_g2.py`), so each seed trains ONE
CPT checkpoint reused by every drill run — a pure refactor with identical results.

- **G-0b ($0, closed-form)**: build SWAP-XOR-B + B(k) constructor; run the full §3 audit suite; exit 0
  blocking. Also: lit-verify the two MEMORY rows (§1) via the three-scan discipline.
- **G-1.5a (band existence — kill-capable, ~1.5h)**: seed-0 CPT + C-dup drills at k ∈ {24, 96, 192}
  (endpoint E[C-dup@384] ≈ 1.0 E-marked from H_007's old-panel 1.0000/0.9323 — re-measured in G-1.5b only
  if a band is found). **KILL (K1) if no k lands f2(C-dup) ∈ [0.60, 0.80]**; ONE refinement drill at a
  geometric midpoint is allowed if the sweep brackets the band (e.g. 24→0.55, 96→0.85 ⇒ try 48), else no
  fishing.
- **G-1.5b (stability, ~1.5h)**: seed-1 CPT + drills at candidate k* and its factor-2 neighbor, both
  seeds ⇒ require ALL FOUR points (2 budgets × 2 seeds) in-band, cross-seed |Δf2(C-dup@k*)| ≤ b = 0.10,
  and the plateau precondition — the knee-not-cliff and across-seeds clauses of the reopen condition,
  verbatim. KILL (K1) otherwise.
- **G-1.5c (contrast pricing, ~1.6h)**: C-scaf and C-shuf at k*, 2 seeds each ⇒ require C-scaf ≤ 0.80
  and C-shuf ≤ 0.80 (F3/F6 headroom). KILL (K1b) otherwise.
- **HARD CAP**: ≤ 8 pre-run drill runs total, then a mandatory OPEN/CLOSE decision — band-hunting beyond
  the cap IS the knee-tuning H_007's rescue analysis warned about; the cap makes it structurally
  impossible.
- **FREEZE**: instantiate every E-anchor from G-1.5 measurements, record B(k) hashes, set
  `pre_register_frozen: true`. No mechanism arm exists before this line.
- **G-2 (verdict, ~1.9h)**: A-tug + C-perm × 2 seeds at k* (4 drills + F4/F5 evals); C-dup, C-scaf,
  C-shuf REUSED byte-identical from G-1.5 (controls trained first are the verdict's control arms —
  no re-run, no replication gap). Collector applies §4 verbatim.

Total ≤ ~6.5h wall, $0 cash, same class as every prior verdict run; first kill at ~1.5h; the mechanism
arm cannot start until the band is measured non-empty, stable, plateaued, and placebo-priced.

## 6. Honest kill criteria

- **K1 (no band)**: no budget yields C-dup f2 ∈ [0.60, 0.80] stably (both seeds, adjacent point, plateau)
  within the run cap ⇒ **CLOSE near-$0** — the reopen door shuts with a MEASURED wall ("the discriminating
  band for a trained control is empty on the budget axis too, at 3.7M/byte/templatic"), the campaign
  result stands unchanged, and the H_008 card seals as CLOSED-NO-BAND.
- **K1b (placebo saturation)**: band exists but C-shuf@k* > 0.80 ⇒ the generic-aux boost alone re-closes
  the headroom ⇒ F6 inadmissible ⇒ CLOSE before the mechanism arm (same sealing).
- **K2 (sample-efficiency null)**: band holds, F1 DEAD both seeds ⇒ conflict buys no sample efficiency ⇒
  the mech-5 FAMILY graduates from unmeasured to MEASURED-NULL at this substrate — a clean red worth
  sealing (upgrades the campaign's disposed map).
- **K3 (aux-transfer, not tension)**: F1 green but F6 fails ⇒ verdict written as "auxiliary
  self-supervision buys few-shot composition at 3.7M" — literature-shaped, PARTIAL, no mechanism claim.
- **K4 (no conflict)**: P-conflict absent at k* ⇒ any green scoped to aux transfer; no tension language.
- **K5 (code absent/epiphenomenal)**: F4 fails in A-tug ⇒ DEAD (v1's seam); F5 fails ⇒ no mechanism-route
  claim; F5 VOID or F4-surplus absent ⇒ green capped at PARTIAL, scoped to "unexplained efficiency gain".
- **Seed-split**: one seed ≥ 0.10, one < 0.05 ⇒ INCONCLUSIVE-CONSOLIDATE, no third-seed fishing, no
  re-run (H_003's 0.1614 Δ-spread priced; H_005 precedent).
- **K-scope (standing)**: even full green claims SAMPLE-EFFICIENCY of an objective-conflict aux signal —
  NOT "tension is the thinking", NOT a reopening of mech-5's forced-code claim (retired), NOT a change to
  mech-1's seal or its PARTIAL thesis boundary. One sentence, everywhere the result is cited.

## 7. Priced-mistake ledger (every place this design touches a paid-for lesson)

| priced mistake | where it would bite H_008 | the built-in fix |
|---|---|---|
| mech-5 control saturation (H_007 G-2: C-dup 1.0000/0.9323 at zero headroom) | F1 void again | G-1.5a/b/c band-existence pre-run at target scale/steps, controls-only, BEFORE any mechanism arm; anchors measured on THIS panel |
| inherited anchor fiction (E[C-dup]=0.62 vs truth 1.00) | frozen arithmetic on sand | no inherited anchors — every E instantiated from G-1.5; the only transfer (E[C-dup@384]≈1.0) is E-marked and non-load-bearing |
| d=64 smoke inversion (+0.073 → −0.0104) | small-scale greenlight lies | H_008 has NO small-scale smoke; all kill-capable reads run at d=384 target scale directly |
| H_005 substrate wall (learned readout: 0.82/0.71 vs 1.0) | a trained resolver in the eval path | scoring stays head-A forced-choice free-slot; probes are F4/F5 diagnostics, never the verdict metric |
| metric-ceiling codebook defect (H_004 GF(2) rank-4) | silent inflation | free-slot + GF(2) audits RE-RUN on SWAP-XOR-B (A1/A2), plus B3 on the smallest budget |
| 2-arm-smoke role-specificity artifact (G-1 lacked C-shuf) | placebo discovered too late | C-shuf priced at k* in G-1.5c BEFORE the mechanism arm; 5 arms in the verdict ledger from the start |
| C-perm per-batch permutation (0.5938/0.5208 flagged) | broken harness | global derangement, frozen per seed |
| H_003 seed spread 0.1614 / knee fragility | band-hunting into noise | band width 0.20 > every measured per-arm spread; 4-point stability + plateau requirement; ≤8-run hard cap |
| byte-family/CPT class side channel (§0.5, NEW) | budget knob rations only part of the channel | SWAP-XOR-B pools byte-orthogonal, non-corpus; A7 audit |
| ungrounded-for-all-arms (H_005 mode) | panel unreachable at small k | coverage-complete grid (f2-subjects ⊆ drill-subjects), B1 audit, floor E[C-dup@k*] ≥ 0.60 |

## 8. Family propagation + registration mechanics

**Scope of any verdict**: the budget axis at 3.7M/byte/Korean-grammar/templatic with synthetic pools. No
outcome reopens mech-5's original forced-code claim (RETIRED stands), touches mech-1's seal, or moves the
thesis boundary past what §6 K-scope allows. The scaled-successor reopen list (≥8M/jamo-word substrate ·
non-templatic register · external parser) is UNCHANGED by any H_008 outcome and stays recorded once.

**Registration**: register as **H_008 · slug `supervision-budget-sample-efficiency`** (identifier free:
the mech-6 gate explicitly minted no H_008; H_006 stays BURNED and unusable). Card created UNFROZEN
(`pre_register_frozen: false`) with freeze blocked on: lit-verify (2 MEMORY rows) → G-0b exit 0 → G-1.5a
→ G-1.5b → G-1.5c. Registry line: "under a scarce, arm-identical, coverage-complete few-shot answer
budget k*, an irreconcilable role-swap co-objective CAUSES higher held-out SWAP-XOR-B composition than
the compute/param/gradient-matched duplicate-objective control — Δd_acc ≥ 0.10 both seeds at a budget
where the trained control is MEASURED in [0.60, 0.80] (G-1.5 band-existence pre-run, mandatory-blocking);
placebo C-shuf and global-derangement harness carried; K1 no-band ⇒ near-$0 close, K2 null ⇒ mech-5
family measured-null." Gates G-0b and the lit-verify are $0; G-1.5a is the first spend (~1.5h wall, $0
cash) and the first kill.
